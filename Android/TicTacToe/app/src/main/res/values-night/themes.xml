<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TicTacToe" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/button_night</item>
        <item name="colorPrimaryVariant">@color/button_night</item>
        <item name="colorOnPrimary">@color/button_text_night</item>
        <!-- Status bar color. -->
        <item name="android:navigationBarColor">@color/background_night</item>
        <item name="android:statusBarColor">@color/background_night</item>
        <!-- Customize your theme here. -->
        <item name="android:background">@color/background_night</item>
    </style>
</resources>